CREATE TABLE Dog (
  url TEXT PRIMARY KEY NOT NULL,
  breed TEXT
);

selectAll:
SELECT * FROM Dog;

insert:
INSERT OR REPLACE INTO Dog(url, breed) VALUES (?,?);

deleteByUrl:
DELETE FROM Dog WHERE url=?;